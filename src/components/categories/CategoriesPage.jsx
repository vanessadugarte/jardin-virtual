import {Container, Typography} from "@mui/material";

const CategoriesPage = () => {
    return (
        <Container maxWidth="sm" sx={{textAlign:"center"}}>
            <Typography variant="h2">Categories page</Typography>
        </Container>
    );
};

export default CategoriesPage;